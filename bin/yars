#!/usr/bin/perl

use FindBin;
use lib "$FindBin::Bin/../lib";

use Yars;
use Clustericious::Commands;

use strict;
use warnings;

$ENV{MOJO_APP} ||= 'Yars';

&main;

sub main {
    my $cmd = $ARGV[0];
    my $app = Yars->new();
    $app = Clustericious::Commands->start;
    $app;
}


